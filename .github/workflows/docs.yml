
name: generate-docs

on:
  push:
    branches: 
      - master

  workflow_dispatch:

jobs:

  generate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        
      - name: install requirements
        run: pip3 install -r requirements.txt
        
      - name: meta/generate-docs.py
        run: python3 meta/generate-docs.py
        
      - name: meta/generate-docs-index.py
        run: python3 meta/generate-docs-index.py
        
      - name: try pyloder
        run: cd meta; ./pyloder.py
        
      - name: Commit files
        run: |
          echo ${{ github.ref }}
          echo ${{ github.sha }}
          git add .
          git config --local user.email "robertbrook@fastmail.fm"
          git config --local user.name "Robert Brook"
          git commit -m "Automated build push" -a | exit 0
          
      - name: Push changes
        if: github.ref == 'refs/heads/master'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: echo lode github folder
        run: echo "https://github.com/ukparliament/ontologies/tree/master/meta/lode"

          
